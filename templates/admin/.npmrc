@frame-me:registry=https://npm.pkg.github.com
# npm 会展开 ${GITHUB_TOKEN}；pnpm 不展开项目级凭证——pnpm 用户请在用户级 ~/.npmrc 配置 token
# （pnpm config set -g "//npm.pkg.github.com/:_authToken" <值>），值从 Frame_Me 仓库 .secrets/github-packages.env 获取
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
