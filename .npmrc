# 本地 Verdaccio 私有 registry（Docker 容器 verdaccio，docker start verdaccio 启动）。
# 安装无需凭证；发布时需带 token：
#   source /tmp/verdaccio-token.env   # 或重新 adduser 获取
#   npm_config_//localhost:4873/:_authToken=$VERDACCIO_TOKEN pnpm -r --filter='./packages/*' publish --no-git-checks
# （token 不写入本文件——git 追踪文件不落凭证）
@frame-me:registry=http://localhost:4873

# --- 以下 GitHub Packages 配置暂时停用，恢复发包时取消注释、注释掉上面 localhost 行 ---
# ⚠️ 注意：GitHub Packages 要求包 scope 与账号/组织同名。
# 当前 @frame-me scope 需要 frame-me 组织存在；若用个人账号发布，需把包名改为 @douhao4648/*
# 或创建 frame-me 组织（发布前的一次性决策，见 README「发包」节）。
# @frame-me:registry=https://npm.pkg.github.com
